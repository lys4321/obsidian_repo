- 로컬 컴퓨터(호스트) 의 포드와 특정 Pod의 포트로 연결하는 명령
- 기본 대상은 Pod 이며 다른 리소스의 이름을 줘도 된다.
- kubectl port-forward 타입명/이름 \[옵션] 호스트포트:Pod포트 
- 옵션의 종류
	- --address
		- 어떤 주소에서 요청을 받을지 설정
		- 기본값은 locahost
		- 여러 개를 지정할 수 있으며 이 경우에는 ','로 구분하여 입력
	- --pod-running-timeout
		- Pod가 Running 상태가 될 때 까지 대기하는 시간 설정
	- 