- 리소스를 전체 교체하는 명령어
	- 여기서 전체라는 의미는 일부 값만 넣는게 아닌 전체 YAML파일로 넣어야한다는 의미
- kubectl replace -f 파일명 \[옵션]
- 옵션
	- --allow-missing-template-keys
	- --cascade
	- --dry-run
	- --field-manager
	- -f
	- --force
	- --grace-period
	- -k(--kustomize)
	- -o(--output)
	- --recursive
	- --raw
		- API 서버에 기본 HTTP PUT 요청을 직접 보내는 옵션
	- --timeout
	- --validate
	- --wait