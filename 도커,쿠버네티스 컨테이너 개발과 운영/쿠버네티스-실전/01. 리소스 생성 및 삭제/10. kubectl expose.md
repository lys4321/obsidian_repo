- kubectl expose (-f 리소스정의파일명 | TYPE NAME) \[옵션]
- k8s 의 리소스를 참조하여 새로운 Service 리소스를 생성하고 노출 시 사용하는 명령어
- 대상 리소스의 label selector를 가져와 재사용
- 옵션
	- --selector
	- --recursive
	- --type
	- --cluster-ip
		- Service의 ClusterIP를 직접 지정
	- -f
	- -k
	- --labels
	- --name