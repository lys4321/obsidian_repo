minikube start
minikube start --cpus 2 --memory 2048mb --driver=docker
minikube ip
kubectl apply -f yaml 파일명
kubectl get pods
minikube service 서비스명 --url
kubectl delete -f yaml파일명
minikube delete

ingress 는 k8s에서 사용하는 l7단계의 접속 표준에 대헌 추상화 계층

ingress yaml ->
ingress controller ->
nginx

ingr resource(yaml 규칙 포함)
api server / etcd(yaml 내 데이터 저장)
ingr controller pods( watcher con -> nginx cont )
service
other pods


Deployment는 Pod 과 그 안에 있는 컨테이너를 포함하는 논리적 영역
Service는  Deployment로 연결을 하게하는 게이트웨이



configmap
secrete 
실습이 필요함


![[Pasted image 20251104160843.png]]