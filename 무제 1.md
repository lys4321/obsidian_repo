docker network create 넷이름
docker network create --subnet 대역 넷이름
docker network ls
docker network inspect 넷이름
docker network connect 넷이름 컨테이너명
docker network disconnect 넷이름 컨테이너명

docker volume create 볼륨명
docker volume ls 
docker volume inspect 볼륨명

docker pull 이미지명
docker build -f 도커파일명 -t 생성할 이미지명
doker tag 원본이미지 만들이미지
docker images


docker container create --name 컨테이너명 이미지명
docker container create -p ~ : ~ --name 컨테이너명 -v 볼륨명 : 안에서 넣을 경로 --network 네트워크명 -e 키=값
docker container start 

docker exec -it 컨테이너명 /bin/bash
docker stop 컨테이너명
docker logs 컨테이너명
docker port 컨테이너명
