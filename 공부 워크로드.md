1. docker 실전 테스트
	- 사용할 이미지들 설정
		- ws : nginx (stateless)
		- db : postgreSQL(persistance)
		- cache db : Reddis(persistance)
		- (+k8s에서 사용 시)nginx-ingress
	- 도커 네트워크 생성
		- localDocker 라는 네트워크 생성
	- 생성한 네트워크 안에서 컨테이너들을 실행
		- nginx 생성
			- nginx 명령어 몇개 사용
			- apt-get update
			- apt-get instal vim 사용
			- /etc/nginx/nginx.conf 설정(+default.conf 대한 것도)
				- stream 블록 설정
					- resolver ip주소 valid=5s 란걸 알음
						- NGINX에게 호스트 이름(예: `postgres-db`)을 IP 주소로 변환하기 위해 **사용할 DNS 서버의 주소**를 알려줍니다
						- 12.0.0.11 도커 내부 dns 주소
						- NGINX가 한 번 성공적으로 확인한 IP 주소를 **5초 동안 캐시**에 보관하고 재사용하도록 지시합니다.
			- nginx -t 와 nginx -s reload 사용
		- postgres
			- psql 명령어 를 사용 해봄
			- create table 실행해봄
			- insert into ~~ 해봄
			- select 해봄
			- 대문자 필수인듯
			- tool 들이 존재하며 디비버를 예롤 들수 있다
		- Redis
			- tool이 존재하며 공식도구인 Redis insight가 있다
			- 정보조사 필요
				- 20251202 기준 굳이 이 문법을 익힐 필요가 있나?
					- 지금 익혀봤자 당장에 못쓰는데?
					- 난이도도 쉽다한다 그럼 나중에 빨리 배울 수 있지 않나?
					- 일단 원리만 대충 파악(완전히는 아니지만)은 했으니 다음으로 넘길까?
	- 사용할 이미지들을 dockerfile로 결합
		- 연결할 세팅값 설정
	- private registry에 저장
2.  compose 실전 테스트
	- yaml 파일 작성
		- document폴더 하위에 만듦
	- 작성한 파일을 기반으로 실행
		- 실제실행
			- 생성 후 중간에 파일내 수정핝다음에 그걸로 삯제를 해도 삭제되는 걸 확인
	- 스케일 관리 시도
		- 포트포오딩 된거라면 스케일 업해도 안돰
		- 컨테이너 이름에 '-1,2,3,4'가 붙어 스케일 조저링 되기 때문에 컨테이너  이름 지정 시 스케틸 관리가 불가
3. k8s 로컬 테스트
	- Pod 설정 예상
		- nginx, redis -> deployment
		- postgres -> statefuleSet
	- Service 설정 예상
		- 외부와의 연결점은 nginx 
		- 내부 연결은 posdtres, redis
	- configmap 설정 예상
		- 아직 없는듯???
	- secrete 설정 예상
		- postregs 환경변수 
	- 로컬에서 파일 작성해서 죄다 올리고 확인완료
	- 반복이 필요하다
4. k8s  실전 테스트
	- 